# me134_4 ros pkg
Code for ME134 project number 4: self-balancing robot.

Launch via `roslaunch me134 simple.launch`. Tuning parameters can be modified via `dynamic_reconfigure`. Control of the torso takes place via the `roslaunch me134 controller.launch` file.
